双副本差异处理记录（2026-08-27）
================================

## 已合并（9 个文件，语法检查全部通过）

### 主 → 副本（主工作区为更新版）
1. scripts/update_completion_batch.py      主版支持双表头候选（"W{n}到课/完课情况"、"W{n}到课/完课状态"），副本仅支持一个
2. scripts/incremental_dingtalk_sheet.py   主版补充尾行清理注释
3. scripts/sync_makeup_sheet.py            主版补充旧补课表 x 占位符说明注释
4. skills/codemao-makeup-reminder/scripts/cancel_makeup_reminder.py  主版 docstring 说明分页读取逻辑
5. scripts/format_makeup_sheet.py          主版动态列宽（row_style(width)）+ class_colors 字典 + 状态判断，副本硬编码 9 列

### 副本 → 主（副本为配置化通用版）
6. scripts/create_post_class_feedback_sheet.py   副本用 {PREFIX} 配置化路径，主版硬编码 0724
7. scripts/update_weekly_invite_followup.py      副本用 {PREFIX} 配置化路径，主版硬编码 0724
8. scripts/prepare_makeup_reminder_from_course_data.py  副本用 __file__ 相对路径，主版硬编码个人绝对路径
9. scripts/update_invite_followup_with_solitaire.py    副本版配置化（class_time_labels 动态计算、data_path roster、Unicode 转义避免编码问题），主版硬编码 3/4 和 new-class-student-list.json

## 保留为实例差异（不合并，各自自洽）
- apps/teacher_workbench/launch.vbs        端口 8765（主）vs 8876（副本）+ 部署层级不同
- scripts/codemao_workbench.py             完整版（主）vs 精简 status-only 版（副本）
- scripts/sync_student_class_times.py      ROSTER_JSON：主 new-class-student-list.json（fetch 硬编码输出），副本 data_path("students_json")（配置化输出）；两边 fetch 脚本输出与读取一致
- scripts/update_saturday_noon_completion.py  CLASS_ID：主 130021（0724 周六午），副本 0（模板未配置）
- scripts/append_live_absence_records_to_dingtalk.py  WORKSPACE：主硬编码个人路径，副本 __file__ 相对路径
- scripts/fetch_group_solitaire_from_crm.mjs  仅尾部换行符差异，无实质内容
- apps/teacher_workbench/server.py          仅尾部换行符差异，无实质内容（Compare-Object 为空）
- scripts/discover_from_capture.py         主版 filter_primary_package（package_id 聚类）vs 副本 filter_primary_group（term/class 数值聚类），功能等价
- skills/codemao-course-data/config.json   实例配置：cookies_file 绝对（主）vs 相对（副本）
- skills/codemao-course-data/config.template.json  实例模板差异
- skills/codemao-makeup-reminder/config.json  实例配置：cookies_file 绝对（主）vs 相对（副本）
- data/fetch-new-class-student-list.mjs    主版硬编码输出 new-class-student-list.json，副本版从 workbench config 读输出路径（配置化）
